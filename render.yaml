services:
  - type: web
    name: skill-specific-backend
    env: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: PORT
        value: 5000
      - key: MONGO_URI
        sync: false
